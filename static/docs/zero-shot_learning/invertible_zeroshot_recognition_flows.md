# Invertible Zero-Shot Recognition Flows

Yuming Shen, Jie Qin, and Lei Huang

## 🧩 Problem to Solve

기존의 제로샷 학습(ZSL)을 위한 심층 생성 모델(예: GAN, VAE)은 고유한 한계를 가지고 있습니다. GAN은 모드 붕괴(mode collapse)와 학습 불안정성에 시달리며 ZSL 관련 정규화자를 적용하기 어렵습니다. VAE는 ELBO(Evidence Lower BOund)를 통한 확률적 근사 최적화로 인해 고품질 미학습(unseen) 샘플을 생성하는 데 한계가 있습니다. 이러한 모델들은 학습 시 학습된(seen) 데이터만 사용하기 때문에, 생성된 미학습 데이터가 학습된 데이터와 유사한 분포를 가지는 '학습된-미학습 편향(seen-unseen bias)' 문제를 효과적으로 해결하지 못합니다. 이 논문은 이러한 한계들을 극복하고 ZSL 성능을 향상시키기 위한 새로운 생성 모델을 제안합니다.

## ✨ Key Contributions

* **최초의 ZSL을 위한 생성형 플로우 모델**: 생성형 플로우 모델을 ZSL에 도입하여, 기존 GAN/VAE의 한계를 극복하는 새로운 인코딩-디코딩 패러다임을 제안합니다.
* **요소화된 조건부 플로우 구조**: 시각적 특징을 의미적 요인($\hat{c}$)과 비의미적 요인($\hat{z}_{f}$)으로 분리하는 새로운 요소화된 조건부 플로우 구조를 제안하며, 이는 정확한 밀도 추정(exact density estimation)을 지원합니다.
* **학습된-미학습 편향 문제 해결 메커니즘**: 생성형 모델의 특성을 활용하여 학습된 데이터와 합성된 미학습 데이터 분포 간의 불일치(discrepancy)를 측정하고 의도적으로 확대함으로써 편향 문제를 명시적으로 해결하는 새로운 메커니즘을 제안합니다.
* **우수한 성능 입증**: 널리 사용되는 ZSL 벤치마크에서 기존 방법론 대비 고전적(Classic) 및 일반화된(Generalized) ZSL 설정 모두에서 상당한 성능 향상을 입증합니다.

## 📎 Related Works

* **제로샷 학습(ZSL)**:
  * **전통적 ZSL**: 시각-의미 투영 학습(예: DeViSE [10], ALE [1], SSE [66]), 의미-시각 투영 학습(예: DEM [65]), 공유 특징 또는 다중 모달 함수 학습(예: ESZSL [41]).
  * **생성형 ZSL**: GAN 기반(예: f-CLSWGAN [59], LisGAN [25]), VAE 기반(예: CVAE-ZSL [36], CADA-VAE [43]), 또는 두 모델의 혼합(예: f-VAEGAN-D2 [61])을 통해 미학습 샘플을 합성하여 분류기 학습에 활용합니다.
* **생성형 플로우 모델**: NICE [6], RealNVP [7], Glow [19] 등 가역 신경망(Invertible Neural Networks, INNs)과 결합하여 효율적인 학습과 샘플 생성을 가능하게 합니다. Conditional Generative Flow [27]와 같이 조건부 생성으로 확장되기도 했습니다.
* **가역 ZSL**: SAE [21]와 같이 문자 그대로 '가역' 투영을 사용하는 ZSL 모델도 있었으나, 샘플 생성이 불가능하고 딥러닝과 결합하기 어려워 IZF와는 차이가 있습니다.

## 🛠️ Methodology

IZF는 가역 신경망(INNs)을 기반으로 한 생성형 플로우 모델을 활용하여 ZSL 문제를 해결합니다.

1. **생성형 플로우 및 INN 개요**:
    * **밀도 추정**: 변수 변환 공식(change of variables formula)을 사용하여 데이터 $x$의 밀도 $p_{\theta}(x)$를 가역 변환 $f$를 통해 잠재 변수 $z$의 사전 확률 $p_Z(f(x))$로 추정합니다.
        $$p_{\theta}(x) = p_Z(f(x))\left|\det \frac{\partial f}{\partial x}\right|$$
    * **결합 계층(Coupling Layers)**: 네트워크 입력을 두 부분으로 나누고 한 부분은 보존하며 다른 부분은 보존된 부분의 함수로 변환하는 구조를 사용하여 가역성과 야코비안 행렬식(Jacobian determinant) 계산 효율성을 보장합니다.

2. **요소화된 조건부 플로우(Factorized Conditional Flow)**:
    * **순방향 패스(Forward Pass) - 의미 요소화**: 시각적 특징 $v$를 입력받아 예측된 의미적 요인 $\hat{c}$과 비의미적 요인 $\hat{z}_f$으로 분리합니다. 여기서 $\hat{c} \in \mathbb{R}^{d_c}$는 데이터의 카테고리 정보와 관련되고, $\hat{z}_f \in \mathbb{R}^{d_v-d_c}$는 비의미적 저수준 특징을 나타내며 레이블 $y$와 독립적이라고 가정합니다.
        $$p_{\theta}(v|y) = p_{C|Y}(\hat{c}|y)p_{Z_f}(\hat{z}_f)\left|\det \frac{\partial f}{\partial v}\right|$$
        여기서 $p_{C|Y}(\hat{c}|y) = \mathcal{N}(\hat{c}|c(y), I)$는 해당 의미 임베딩 $c(y)$를 중심으로 하는 가우시안 분포이고, $p_{Z_f}(\hat{z}_f) = \mathcal{N}(\hat{z}_f|0, I)$입니다.
    * **역방향 패스(Reverse Pass) - 조건부 샘플 생성**: 의미 임베딩 $c$와 비의미적 잠재 변수 $z_f$를 입력받아 시각적 샘플 $\hat{v}$를 생성합니다.
        $$\hat{v} = f^{-1}([c, z_f])$$

3. **훈련 전략**:
    * **인코딩을 통한 디코딩 학습 ($L_{\text{Flow}}$)**: 순방향 패스의 분석적 로그-우도 최대화(analytic log-likelihood maximization)를 통해 학습된 클래스의 시각적 특징 $v_s$에 대한 조건부 확률 $p_{\theta}(v_s|y_s)$를 최대화합니다.
        $$L_{\text{Flow}} = E_{(v_s, y_s, c_s)}[-\log p_{\theta}(v_s|y_s)]$$
    * **분류 프로토타입 중심화 ($L_C$)**: 역방향 패스를 통해 각 클래스 의미 표현 $c_s$와 0으로 구성된 비의미적 요인으로 생성된 '분류 프로토타입' $f^{-1}([c_s, 0])$이 해당 클래스 시각 샘플의 평균 $\bar{v}_s^c$에 가깝도록 정규화합니다.
        $$L_C = E_{(c_s, \bar{v}_s^c)}\left[\left\|f^{-1}([c_s, 0]) - \bar{v}_s^c\right\|^2\right]$$
    * **학습된-미학습 편향 측정 및 완화 ($L_{\text{iMMD}}$)**: 합성된 미학습 시각 분포 $p_{\hat{V}_u}$가 실제 학습된 시각 분포 $p_{V_s}$와 충돌하는 것을 방지하기 위해, MMD(Maximum Mean Discrepancy)의 음수 값을 최소화하여 두 분포 간의 차이를 의도적으로 크게 만듭니다.
        $$L_{\text{iMMD}} = -\text{MMD}(p_{V_s}||p_{\hat{V}_u})$$
        여기서 $\hat{v}_u^i = f^{-1}([c_u^i, z_f^i])$로 합성된 미학습 샘플을 사용합니다.
    * **전체 목적 함수**: 세 가지 손실 함수를 가중치 $\lambda_1, \lambda_2, \lambda_3$와 함께 결합합니다.
        $$L_{\text{IZF}} = \lambda_1 L_{\text{Flow}} + \lambda_2 L_C + \lambda_3 L_{\text{iMMD}}$$

4. **제로샷 인식**:
    * **IZF-NBC (Naive Bayesian Classifier)**: 테스트 샘플 $v_q$와 각 클래스의 분류 프로토타입 $f^{-1}([c(y),0])$ 간의 유클리드 거리를 비교하여 가장 가까운 클래스로 분류합니다.
    * **IZF-Softmax**: 합성된 미학습 데이터로 학습된 별도의 분류기(held-out classifier)를 사용하여 Softmax 활성화 위에 구축된 단일 계층 완전 연결 네트워크를 통해 분류합니다.

## 📊 Results

* **모의실험(Toy Experiments)**: 4개의 클래스 중 1개를 미학습 클래스로 설정한 2D 데이터셋에서 IZF가 GAN/VAE 기반 모델보다 $L_{\text{iMMD}}$와 더 안정적으로 작동하며, 미학습 클래스와 학습된 클래스 간의 관계를 성공적으로 해석함을 보였습니다. $L_{\text{iMMD}}$가 없는 경우 모델이 학습된 클래스에 편향되는 반면, 과도하게 큰 $L_{\text{iMMD}}$는 비현실적인 샘플을 생성하여 성능 저하를 일으킬 수 있음을 확인했습니다. 양의 MMD를 사용하면 편향 문제가 심화됩니다.
* **실제 데이터 실험 (GZSL)**: AwA1, AwA2, CUB, SUN, aPY 데이터셋에서 IZF-Softmax가 기존 SOTA(State-of-the-Art) 모델들을 전반적으로 능가하는 성능을 보였습니다. 특히 AwA 데이터셋에서 상당한 성능 차이를 나타냈습니다. IZF-NBC도 AwA에서 좋은 성능을 보였습니다. IZF는 학습된 데이터와 미학습 데이터 간의 균형 잡힌 정확도를 달성하며, 특히 미학습 정확도($A_u$)가 크게 향상되어 $L_{\text{iMMD}}$의 효과를 입증했습니다.
* **실제 데이터 실험 (CZSL)**: CZSL 설정에서도 IZF-Softmax와 IZF-NBC 모두 기존 방법론보다 우수한 per-class 정확도를 달성하며, 학습된 개념에서 미학습 개념으로의 지식 변환 능력이 뛰어남을 보여주었습니다.
* **구성 요소 분석**: AwA1 데이터셋에서 각 손실 항($L_{\text{Flow}}$, $L_C$, $L_{\text{iMMD}}$)의 중요성을 입증했습니다. 특히 $L_{\text{iMMD}}$가 없으면 분류 결과가 학습된 개념에 심각하게 편향되는 것을 확인했습니다. 양의 MMD를 적용하면 미학습 정확도가 급격히 하락했습니다. 커널 선택(IM vs. Gaussian)은 IZF 성능에 큰 영향을 미치지 않았습니다.
* **하이퍼파라미터 분석**: $L_{\text{Flow}}$의 가중치($\lambda_1$)는 가장 높은 값을 가질 때 최고의 성능을 보였습니다. $L_{\text{iMMD}}$의 가중치($\lambda_3$)는 너무 크면 네트워크가 과적합되어 비현실적인 샘플을 생성하고 $L_{\text{Flow}}$의 기능을 방해할 수 있음을 확인했습니다. IZF는 일반적으로 20 에포크 내외에서 최적의 성능을 달성하여 효율적인 학습 속도를 보였습니다.
* **미학습 클래스 판별 능력**: t-SNE 시각화 결과, IZF가 미학습 클래스에 대해 직접적인 정규화 손실 없이도 의미론적으로 구별 가능한 샘플을 성공적으로 생성함을 보여주었습니다.

## 🧠 Insights & Discussion

* **생성형 플로우의 이점**: IZF는 생성형 플로우 모델의 가역적인 특성을 활용하여 시각 공간과 의미 공간 간의 양방향 매핑을 동일한 네트워크 파라미터로 수행할 수 있습니다. 이는 기존 GAN/VAE 모델의 불안정성과 낮은 생성 품질 문제를 해결하며, ZSL에 필수적인 고품질의 미학습 샘플 생성을 가능하게 합니다. 또한, 명시적인 로그-우도 최대화 목적 함수를 통해 보다 안정적인 학습을 제공합니다.
* **요소화된 표현의 효과**: 순방향 패스에서 시각적 정보를 의미적 요인과 비의미적 요인으로 분리함으로써, 모델은 클래스 간 유사성 지식을 명확하게 반영하고 조건부 샘플 생성을 보다 효과적으로 수행할 수 있게 됩니다. 이는 ZSL의 핵심인 학습된-미학습 일반화에 기여합니다.
* **편향 문제에 대한 명시적 해결**: $L_{\text{iMMD}}$를 통해 학습된 데이터 분포와 합성된 미학습 데이터 분포 간의 불일치를 적극적으로 최대화함으로써, IZF는 기존 생성형 ZSL 모델의 고질적인 문제인 학습된-미학습 편향을 효과적으로 완화합니다. 이는 미학습 클래스의 정확도를 크게 향상시키는 주요 요인입니다.
* **한계점**: $L_{\text{iMMD}}$의 가중치 $\lambda_3$가 너무 높으면 네트워크가 $L_{\text{iMMD}}$ 값에 지나치게 집중하여 비현실적인 샘플을 생성할 수 있습니다. IZF-Softmax는 추가적인 분류기 학습을 위해 컴퓨팅 자원이 더 필요합니다.

## 📌 TL;DR

이 논문은 제로샷 학습(ZSL)에서 학습된-미학습 편향 문제와 기존 생성 모델(GAN, VAE)의 한계를 극복하기 위해 **가역 제로샷 플로우(Invertible Zero-shot Flow, IZF)**를 제안합니다. IZF는 가역 신경망 기반의 생성형 플로우 모델로, 순방향 패스를 통해 시각적 특징을 의미적/비의미적 요인으로 분리하고, 역방향 패스를 통해 조건부 시각 샘플을 생성합니다. 학습 과정에서 (1) 정확한 밀도 추정을 위한 인코딩 손실 $L_{\text{Flow}}$, (2) 분류 프로토타입 중심화를 위한 $L_C$, 그리고 (3) 학습된-미학습 데이터 분포 간의 불일치를 확대하는 **음수 MMD 기반 손실 $L_{\text{iMMD}}$**를 사용하여 편향 문제를 명시적으로 해결합니다. IZF는 벤치마크 데이터셋에서 SOTA 성능을 달성하며, 특히 $L_{\text{iMMD}}$를 통해 미학습 클래스의 인식 정확도를 크게 향상시켰습니다.

# Overcoming Forgetting in Federated Learning on Non-IID Data

Neta Shoham, Tomer Avidor, Aviv Keren, Nadav Israel, Daniel Benditkis, Liron Mor-Yosef, Itai Zeitak

## 🧩 Problem to Solve

본 논문은 비동일 분포(non-IID) 데이터 환경에서 연합 학습(Federated Learning, FL)이 겪는 문제를 다룹니다. 특히, 로컬 모델들이 비동일 데이터로 인해 서로 멀어지며(drift apart) 학습을 저해하고, 이는 전통적인 독립동일분포(IID) 데이터 환경에 비해 정확도 저하 및 더 많은 통신 라운드를 필요하게 만듭니다. 또한, 로컬 훈련 반복 횟수(E)가 너무 높을 때 성능이 저하되는 현상도 해결하고자 합니다. 이는 다중 작업 학습(Multi-Task Learning) 또는 평생 학습(Lifelong Learning)에서 발생하는 "파국적 망각(catastrophic forgetting)" 문제와 유사한 맥락으로 분석됩니다.

## ✨ Key Contributions

* **FedCurv 알고리즘 제안**: 평생 학습(Lifelong Learning)의 파국적 망각을 극복하는 Elastic Weight Consolidation (EWC) 기법을 연합 학습 환경에 맞게 변형한 FedCurv를 제안합니다.
* **선택적 파라미터 강성 부여**: 어텐션 메커니즘과 유사하게 Fisher 정보 행렬의 대각 성분(diagonal of the Fisher information matrix)을 사용하여 모델 파라미터에 선택적인 강성(stiffness)을 부여함으로써, 로컬 모델들이 공통의 최적점으로 수렴하도록 유도하고 모델 이탈(drift)을 방지합니다.
* **효율적인 통신 및 개인 정보 보호**: 추가적인 통신 대역폭을 크게 늘리거나 개인 정보 보호 위험을 가중시키지 않으면서 이 방법을 효율적으로 구현할 수 있음을 보여줍니다. 기존 FedAvg와 유사한 방식으로 집계된 정보를 교환합니다.
* **성능 향상 입증**: MNIST 데이터셋을 사용한 비동일 분포 환경 실험에서 FedAvg 및 FedProx와 같은 기존 경쟁 알고리즘보다 우수한 성능(더 빠른 수렴 및 더 높은 정확도)을 입증했습니다. 특히 로컬 훈련 반복 횟수(E)가 높을 때 더욱 효과적입니다.

## 📎 Related Works

* **FedAvg [1]**: 연합 학습의 기본적인 알고리즘으로, 비동일 분포 데이터 및 높은 로컬 훈련 반복 횟수(E)에서 성능 저하 문제를 겪습니다.
* **데이터 공유 기법 [6]**: 일부 데이터 샘플을 노드 간에 공유하여 정확도를 높이지만, 연합 학습의 주요 목적인 데이터 프라이버시를 침해합니다.
* **MOCHA [10]**: 다중 작업 학습(Multi-Task Learning) 접근 방식으로, 각 작업에 다른 파라미터를 연결하고 손실 항을 추가하지만, 심층 신경망에는 적합하지 않습니다.
* **FedProx [11]**: 본 연구와 유사하게 파라미터 강성(parameter stiffness)을 사용하는 페널티 항 $$\frac{1}{2}\mu\Vert\theta - \theta_{t}\Vert$$을 도입하지만, 페널티가 등방성(isotropic)이어서 모델 파라미터의 유연성이 제한됩니다.
* **DANE [12], AIDE [13]**: FedProx를 보완하거나 inexact DANE을 사용하여 수렴을 가속화하지만, 비동일 분포 데이터에 대한 견고성이 부족하거나 로컬 계산량을 제한합니다.
* **Elastic Weight Consolidation (EWC) [7]**: 본 논문의 FedCurv 알고리즘의 핵심적인 영감으로, 파국적 망각을 극복하기 위해 Fisher 정보 행렬을 사용하여 중요한 파라미터 변경에 페널티를 부여합니다.

## 🛠️ Methodology

본 논문은 EWC(Elastic Weight Consolidation) 알고리즘을 연합 학습 시나리오에 맞게 변형한 FedCurv를 제안합니다.

1. **영감**: 평생 학습에서 이전 작업에 중요한 파라미터들을 식별하고, 새로운 작업을 학습할 때 이 파라미터들의 변화에 페널티를 부과하여 파국적 망각을 방지하는 EWC에서 착안했습니다. Fisher 정보 행렬의 대각 성분이 파라미터의 중요도를 나타내는 데 사용됩니다.

2. **수정된 손실 함수**: 각 통신 라운드 $t$에서, 노드 $s$는 다음과 같은 손실 함수를 최적화합니다.
    $$ \tilde{L}_{t,s}(\theta) = L_s(\theta) + \lambda \sum_{j \in S \setminus s} (\theta - \hat{\theta}_{t-1,j})^{\text{T}} \text{diag}(\hat{I}_{t-1,j})(\theta - \hat{\theta}_{t-1,j}) $$
    * $L_s(\theta)$는 노드 $s$의 로컬 손실입니다.
    * $\hat{\theta}_{t-1,j}$는 이전 라운드 $t-1$에서 노드 $j$의 모델 파라미터입니다.
    * $\text{diag}(\hat{I}_{t-1,j})$는 노드 $j$의 Fisher 정보 행렬 $\hat{I}_{t-1,j}$의 대각 성분으로, 각 파라미터의 중요도를 나타냅니다.
    * 페널티 항은 로컬 모델 $\theta$가 다른 노드들의 이전 모델 $\hat{\theta}_{t-1,j}$로부터 Fisher 정보에 의해 가중된 방식으로 너무 많이 벗어나지 않도록 강제하여, 모델 이탈을 방지하고 공통의 최적점으로 수렴하도록 돕습니다.

3. **학습 및 통신 과정**:
    * 각 라운드 $t$에서, 모든 노드는 초기화된 공통 모델 $\hat{\theta}_t = \frac{1}{N} \sum_{N}_{i=1} \hat{\theta}_{t-1,i}$로부터 시작합니다.
    * 각 노드 $s$는 자신의 로컬 데이터와 수정된 손실 함수 $\tilde{L}_{t,s}(\theta)$를 사용하여 $E$ 로컬 에포크 동안 SGD를 실행합니다.
    * 라운드 $t$가 끝날 때, 각 노드 $j$는 업데이트된 모델 $\hat{\theta}_{t,j}$와 Fisher 정보 행렬의 대각 성분 $\text{diag}(\hat{I}_{t,j})$를 다른 노드(또는 중앙 서버)에 전송합니다.

4. **대역폭 및 개인 정보 보호**:
    * 손실 함수는 다음과 같이 재배열할 수 있습니다:
        $$ \tilde{L}_{t,s}(\theta) = L_s(\theta) + \lambda\theta^{\text{T}}\left[\sum_{j\in S\setminus s}\text{diag}(\hat{I}_{t-1,j})\right]\theta - 2\lambda\theta^{\text{T}}\sum_{j\in S\setminus s}\text{diag}(\hat{I}_{t-1,j})\hat{\theta}_{t-1,j} + \text{const} $$
    * 따라서 중앙 서버는 $\theta$와 동일한 크기의 두 가지 추가 요소 $u_t = \sum_{j \in S} \text{diag}(\hat{I}_{t-1,j})$ 및 $v_t = \sum_{j \in S} \text{diag}(\hat{I}_{t-1,j})\hat{\theta}_{t-1,j}$만 유지하고 전송하면 됩니다.
    * 각 장치 $j$도 $\text{diag}(\hat{I}_{t-1,j})$와 $\text{diag}(\hat{I}_{t-1,j})\hat{\theta}_{t-1,j}$와 동일한 크기의 두 가지 추가 요소만 전송하면 됩니다.
    * 이는 FedAvg와 유사한 방식으로 집계된 정보를 교환하므로, 통신 대역폭을 크게 늘리지 않으며 개인 정보 보호 측면에서도 FedAvg와 크게 다르지 않습니다. FedAvg에 적용된 보안 집계 방법도 FedCurv에 적용될 수 있습니다.

## 📊 Results

* **실험 환경**: 96개의 시뮬레이션 장치 그룹에서 MNIST 데이터셋을 사용하여 실험을 수행했습니다. 데이터는 각 장치에 2개의 동질 레이블 블록을 무작위로 할당하여 비동일(non-IID) 분포를 만들었습니다. CNN 아키텍처를 사용했습니다.
* **주요 비교**: FedCurv (제안 알고리즘)와 FedAvg, FedProx를 비교했습니다. 로컬 에포크 수 $E$의 영향도 탐색했습니다.
* **정확도 및 수렴 속도 (표 1)**:
  * **$E=50$ (높은 로컬 에포크)**: FedCurv는 90% 테스트 정확도 달성까지 FedAvg보다 3배 빨랐습니다 (FedCurv: 69라운드, FedAvg: 221라운드). FedProx도 FedAvg보다 빨랐지만, 95% 정확도에서는 FedAvg보다 2배 느렸던 반면 FedCurv는 2배 빨랐습니다.
  * **$E=10$ (낮은 로컬 에포크)**: FedCurv와 FedProx 모두 개선 효과가 덜했지만, FedCurv가 여전히 다른 알고리즘들을 능가했습니다.
* **학습 곡선 (그림 1, 2)**:
  * FedProx와 FedCurv 모두 훈련 초기에 좋은 성능을 보입니다.
  * 그러나 FedProx는 등방성 페널티(isotropic penalty)로 인해 파라미터의 유연성이 부족하여 고정확도 달성에 한계가 있었습니다.
  * FedCurv는 Fisher 정보 행렬을 통한 적응형 강성(adaptive stiffness)으로 파라미터에 충분한 유연성을 제공하여 최종적으로 더 높은 정확도에 도달했습니다.
  * 이론적 예측대로, FedCurv는 $E$ 값이 높을수록 더 큰 성능 향상을 보였습니다.

## 🧠 Insights & Discussion

* **연합 학습과 평생 학습의 연결**: 본 연구는 비동일 분포 데이터 환경에서의 연합 학습 문제와 평생 학습에서의 파국적 망각 문제 사이의 깊은 유사성을 성공적으로 활용했습니다. EWC의 아이디어를 연합 학습에 적용함으로써 로컬 모델 간의 이탈을 효과적으로 방지하고 공통의 최적점으로 수렴하도록 유도했습니다.
* **적응형 페널티의 중요성**: FedCurv의 핵심은 Fisher 정보 행렬의 대각 성분을 사용하여 각 파라미터의 중요도에 따라 선택적으로 페널티를 부여하는 것입니다. 이는 FedProx의 등방성 페널티와 대조되며, 모델이 중요한 지식을 보존하면서도 새로운 정보에 유연하게 적응할 수 있도록 하여 더 높은 최종 정확도를 달성하게 합니다. 특히, FedCurv는 모델이 파라미터 공간에서 '길을 잃지 않도록' 가이드하면서도 충분한 탐색의 자유를 부여합니다.
* **높은 로컬 에포크의 이점**: 기존 FedAvg에서 높은 로컬 에포크는 비동일 데이터 환경에서 성능 저하를 야기하는 것으로 알려져 있습니다. FedCurv는 이러한 문제를 해결하여 높은 로컬 에포크를 사용하여 통신 라운드를 줄이면서도 성능을 향상시키는 것이 가능함을 보여주었습니다. 이는 실제 연합 학습 환경에서 통신 비용을 줄이는 데 큰 이점이 됩니다.
* **실용적인 적용 가능성**: 제안된 방법은 대역폭 사용량을 크게 늘리지 않고 개인 정보 보호 이점을 유지하기 때문에 실제 연합 학습 시나리오에 적용하기에 매우 실용적입니다.

## 📌 TL;DR

비동일 분포 데이터 환경에서 연합 학습(FL)의 모델 이탈과 성능 저하 문제를 해결하기 위해, 본 논문은 평생 학습의 EWC(Elastic Weight Consolidation)에서 영감을 받은 **FedCurv** 알고리즘을 제안합니다. FedCurv는 로컬 손실 함수에 페널티 항을 추가하며, 이 페널티는 Fisher 정보 행렬의 대각 성분을 사용하여 각 파라미터의 중요도에 따라 선택적으로 가중치를 부여함으로써 로컬 모델들이 공통의 최적점으로 수렴하도록 유도합니다. 실험 결과, FedCurv는 비동일 MNIST 데이터셋에서 FedAvg 및 FedProx보다 훨씬 빠르고 정확한 수렴을 보였으며, 특히 로컬 훈련 에포크 수가 많을 때 탁월한 성능을 나타냈습니다. 이는 통신 효율성과 개인 정보 보호를 유지하면서 연합 학습의 핵심 과제를 해결하는 효과적인 방법임을 입증합니다.

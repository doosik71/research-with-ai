# Real-World Image Datasets for Federated Learning

Jiahuan Luo, Xueyang Wu, Yun Luo, Yunfeng Huang, Yang Liu, Aubu Huang, Qiang Yang

## 🧩 Problem to Solve

연합 학습(Federated Learning, FL) 연구는 크게 발전했지만, 대부분의 기존 연구는 미리 존재하는 공개 데이터셋과 인위적인 파티션을 사용하여 데이터 연합을 시뮬레이션합니다. 이로 인해 실제 엣지 애플리케이션에서 생성된 고품질의 레이블링된 데이터가 부족하여 연합 학습 벤치마크 및 모델 평가의 발전이 지연되고 있습니다. 특히, 실제 연합 학습 시나리오의 특징인 비독립동일분포(non-IID) 및 불균형한 데이터 분포를 반영하는 데이터셋의 부재가 큰 문제입니다.

## ✨ Key Contributions

* **실제 연합 학습 이미지 데이터셋 공개**: 26개의 거리 카메라에서 수집된 900개 이상의 이미지와 7가지 객체 카테고리에 대한 상세한 바운딩 박스(bounding box) 주석을 포함하는 실제 데이터셋을 소개합니다. 이 데이터셋은 실제 연합 학습 환경의 특징인 non-IID 및 불균형한 데이터 분포를 반영합니다.
* **연합 객체 감지 벤치마크 제공**: 이 데이터셋을 기반으로 주류 객체 감지 알고리즘인 YOLOv3와 Faster R-CNN을 연합 학습 환경에서 구현하고, 모델 성능, 효율성, 통신 비용에 대한 광범위한 벤치마크를 제공합니다.
* **공개 사용 가능성**: 데이터셋과 알고리즘 코드를 공개하여 연합 학습 연구 커뮤니티의 발전에 기여합니다.

## 📎 Related Works

* **연합 학습 시스템 설계**: Google의 대규모 연합 학습 시스템 설계 개요(Bonawitz et al., 2019).
* **연합 학습 프레임워크**: TensorFlow Federated (TFF), WeBank의 Federated AI Technology Enabler (FATE), PyTorch 기반의 OpenMined (Ryffel et al., 2018) 라이브러리 등이 언급됩니다.
* **연합 학습 벤치마크**: "자연스러운" 데이터 파티션을 제공하는 LEAF (Caldas et al., 2018) 프레임워크.
* **연합 학습 알고리즘 및 응용**: LSTM (McMahan et al., 2017), SVM (Rubinstein et al., 2009), Gradient Boosting Trees (Cheng et al., 2019), 다음 단어 예측 (Hard et al., 2018), 음성 키워드 감지 (Leroy et al., 2018), 이미지 분류 (Liu et al., 2018) 등의 기존 연구를 언급합니다.
* **객체 감지 알고리즘**: Faster R-CNN (Ren et al., 2015)과 YOLOv3 (Redmon and Farhadi, 2018a)와 같은 최신 기술이 벤치마크 알고리즘으로 사용됩니다.

## 🛠️ Methodology

1. **Street Dataset 생성**:
    * **데이터 수집**: 26개의 거리 모니터링 비디오에서 956개의 이미지($704 \times 576$ 픽셀)를 무작위로 캡처하여 유사하거나 야간 장면 이미지를 제거한 후 사용합니다.
    * **객체 주석**: 바구니(Basket), 상자(Carton), 의자(Chair), 전동차(Electromobile), 가스통(Gastank), 차양(Sunshade), 테이블(Table)의 7가지 객체 카테고리에 대해 바운딩 박스($x_{min}, y_{min}, x_{max}, y_{max}$)를 사용하여 주석을 달았습니다. 총 2,544개의 객체 주석이 있습니다.
    * **데이터 특징**: 클래스 분포가 불균형하고, 이미지당 객체 빈도가 다양하여 실제 환경의 non-IID 특성을 반영합니다.
    * **데이터 분할**: 전체 데이터셋을 약 80% (765개 이미지) 훈련 세트와 20% (191개 이미지) 테스트 세트로 나눕니다. 테스트 세트는 소수의 이미지를 가진 6개 카메라와 다른 20개 카메라에서 무작위 샘플링된 이미지로 구성되어, 기존 카메라의 예측 가능성과 미등록 카메라의 일반화 능력을 함께 평가할 수 있도록 합니다.
2. **클라이언트 데이터 분할**: 훈련 세트를 카메라의 지리적 정보에 따라 두 가지 시나리오로 나눕니다.
    * **Street-20**: 각 카메라 장치를 개별 클라이언트(총 20개 클라이언트)로 설정하여 각 장치에서 연합 학습 알고리즘이 실행되는 시나리오를 모의합니다.
    * **Street-5**: 지리적으로 인접한 카메라들을 그룹화하여 5개의 클라이언트를 형성합니다. 이는 여러 엣지 장치를 중앙 노드가 제어하는 시나리오나, 다른 보안 회사와 같이 여러 카메라를 소유한 주체가 연합 학습에 참여하는 시나리오를 모의합니다.
3. **연합 학습 구현**:
    * **기본 알고리즘**: Federated Averaging (FedAvg) 알고리즘 (McMahan et al., 2016)을 "Pseudo FedAvg"로 수정하여 사용했습니다. 이는 서버-클라이언트 통신 프레임워크(예: SocketIO) 대신 하드 드라이브에 체크포인트를 저장하고 복원하는 방식으로 모델 집계를 단순화했습니다.
    * **객체 감지 모델**: YOLOv3 (Darknet-53 백본)와 Faster R-CNN (VGG16 백본)을 PyTorch (Paszke et al., 2017)로 구현했습니다. Faster R-CNN은 수렴 속도를 높이기 위해 사전 학습된 VGG16 모델을 사용했습니다.
    * **실험 파라미터**: $C$ (각 라운드에 참여하는 클라이언트 수), $E$ (각 클라이언트가 로컬 데이터셋에서 수행하는 훈련 횟수), $B$ (미니 배치 크기)를 조정하여 실험했습니다. 모든 클라이언트가 훈련에 참여하도록 $R=1$로 고정했습니다.
4. **평가 지표**:
    * **IOU (Intersection Over Union)**: 예측 바운딩 박스와 실제 바운딩 박스 간의 겹침을 평가합니다. $IOU = \frac{\text{area}(B_{gt} \cap B_p)}{\text{area}(B_{gt} \cup B_p)}$
    * **mAP (mean Average Precision)**: PASCAL VOC 2010 표준을 사용하여 클래스별 AP의 평균으로 성능을 측정했습니다. mAP 계산 시 IOU 임계값은 0.5로 설정했습니다.
    * **통신 비용**: 목표 mAP에 도달하는 데 필요한 통신 라운드 수 및 데이터 전송량(MB)을 측정했습니다.

## 📊 Results

* **로컬 에폭($E$)의 효과**: 로컬 에폭($E$)을 증가시키면 초기 훈련 절차에서 더 높은 mAP에 도달하여 수렴 속도가 빨라지는 것을 확인했습니다.
* **Non-IID 데이터의 영향**: Street-20 데이터셋($C=20$, 더 높은 non-IID)은 Street-5 데이터셋($C=5$, 상대적으로 낮은 non-IID)에 비해 성능이 낮았습니다. 이는 non-IID 데이터셋에서 FedAvg가 중앙 집중식 훈련만큼의 성능을 달성하기 어렵다는 것을 보여줍니다.
* **사전 학습 모델의 중요성**: 사전 학습된 모델 가중치로 초기화하면 특히 각 클라이언트의 데이터 양이 적은 Street-20 데이터셋에서 성능이 크게 안정적으로 향상되었습니다. 이는 작은 탐지 데이터셋에 대한 미세 조정에 대규모 데이터셋 사전 학습이 중요하다는 것을 시사합니다.
* **배치 크기의 영향**: 각 클라이언트의 배치 크기를 늘리는 것은 항상 효과적이지 않았으며, 특히 Street-20처럼 각 클라이언트의 이미지 수가 적을 때는 오히려 성능 저하를 초래하기도 했습니다.
* **모델별 성능**: Faster R-CNN (사전 학습된 VGG-16 백본)의 경우 $C=5, E=1$ 설정에서 FedAvg 모델이 중앙 집중식 훈련과 거의 동일한 성능에 도달했습니다.
* **훈련 손실**: FedAvg는 훈련 손실 최적화 및 일반화 성능 모두에 효과적이었습니다. 큰 로컬 에폭($E$)은 일반적으로 더 작은 손실과 부드러운 훈련 손실 곡선을 생성하지만, 결국 비슷한 mAP에 도달했습니다.
* **통신 비용**: Faster R-CNN은 YOLOv3에 비해 파라미터 크기가 두 배 이상 커서 동일한 목표 mAP에 도달하는 데 더 많은 통신량과 라운드가 필요했습니다.

## 🧠 Insights & Discussion

* **실제 데이터셋의 가치**: 본 연구에서 공개한 Street Dataset은 실제 환경의 non-IID 및 불균형한 데이터 분포를 잘 반영하여 연합 학습의 현실적인 벤치마크로서 큰 가치를 지닙니다. 이는 연구자들이 non-IID 문제 해결을 위한 연합 학습 알고리즘을 개발하고 평가하는 데 중요한 기반이 될 것입니다.
* **Naive FedAvg의 놀라운 성능**: 객체 감지와 같은 복잡한 태스크에서 클라이언트 모델 파라미터를 단순히 평균화하는 naive FedAvg가 non-IID 데이터셋에서도 상당한 성능을 달성할 수 있음을 보여주었습니다. 이는 연합 학습이 실제 비전 태스크에 적용될 가능성을 높입니다.
* **사전 학습의 중요성**: 클라이언트별 데이터가 적은 시나리오에서 사전 학습된 모델 사용은 성능과 안정성에 결정적인 영향을 미칩니다.
* **확장 및 개선 방향**: 향후 데이터셋을 지속적으로 보강하고 더 많은 벤치마크를 제시할 예정입니다. 또한, non-IID 문제 해결을 위한 더욱 정교한 연합 학습 알고리즘 연구가 필요함을 시사합니다.

## 📌 TL;DR

이 논문은 연합 학습(FL)을 위한 실제 이미지 데이터셋의 부족과 non-IID 데이터 분포 문제에 대응하기 위해, 26개 거리 카메라에서 수집된 900개 이상의 이미지와 7가지 객체 카테고리 바운딩 박스 주석을 포함하는 'Street Dataset'을 공개합니다. 이 데이터셋은 실제 연합 학습 시나리오의 non-IID 및 불균형 특성을 반영합니다. 연구팀은 이 데이터셋을 사용하여 YOLOv3와 Faster R-CNN이라는 두 가지 주요 객체 감지 알고리즘을 연합 학습 환경에서 벤치마킹했으며, 로컬 에폭 증가, 사전 학습 모델 사용이 성능 향상에 중요함을 발견했습니다. 또한, 단순한 FedAvg 방식도 non-IID 환경에서 객체 감지 작업에 대해 상당한 성능을 보임을 입증하며, 통신 비용 분석도 제공합니다. 이 데이터셋과 코드는 연합 학습 커뮤니티가 non-IID 문제 해결 및 실제 비전 태스크 적용 연구를 진행하는 데 중요한 기반을 제공합니다.

# Parallel Distributed Logistic Regression for Vertical Federated Learning without Third-Party Coordinator

Shengwen Yang, Bing Ren, Xuhui Zhou, Liping Liu

## 🧩 Problem to Solve

분산된 데이터로 모델을 훈련하는 연합 학습(Federated Learning)은 데이터 프라이버시를 보호하면서 대규모 데이터 코퍼스에서 지식을 추출하는 데 중요합니다. 특히, 수직 연합 학습(Vertical Federated Learning, VFL)은 다른 특성 공간을 가지지만 동일한 샘플 ID를 공유하는 데이터셋에 적용됩니다.

기존의 VFL 로지스틱 회귀 구현 방식은 다음과 같은 문제점을 안고 있습니다:

* **신뢰할 수 있는 제3자 조정자(coordinator) 필요**: 당사자 A와 B 외에, 두 당사자 모두에게 신뢰받는 제3자 조정자 C가 모델 훈련 과정에 필수적으로 참여합니다. 이러한 조정자를 찾는 것은 현실적으로 매우 어려우며, 시스템 복잡도를 증가시키고 데이터 유출 위험을 높입니다.
* **대규모 데이터 처리의 어려움**: 기존 시스템은 각 당사자에서 단일 머신으로 실행되는 경우가 많아, 대용량 훈련 데이터를 효율적으로 처리하기 위한 병렬 분산 시스템 설계가 부족합니다.

이 논문은 제3자 조정자 없이 수직 연합 학습을 위한 로지스틱 회귀 모델을 병렬 분산 방식으로 효율적으로 훈련하는 해결책을 제시합니다.

## ✨ Key Contributions

* **제3자 조정자 없는 새로운 VFL 아키텍처 제안**: 시스템 복잡도를 크게 줄이고, 신뢰할 수 있는 조정자의 도움 없이 두 당사자가 직접 공동 모델을 훈련할 수 있도록 하는 아키텍처를 제안합니다.
* **대규모 데이터 처리를 위한 병렬 분산 로지스틱 회귀 구현**: 각 당사자의 머신 클러스터에서 훈련 알고리즘을 실행하여 대용량 훈련 데이터를 처리할 수 있는 병렬 분산 로지스틱 회귀를 구현합니다.
* **다자간 모델 훈련으로의 확장성**: 제안된 시스템 아키텍처는 두 당사자 간의 모델 훈련뿐만 아니라 다자간 모델 훈련으로도 쉽게 확장될 수 있습니다.

## 📎 Related Works

* **연합 학습(Federated Learning, FL)**: 데이터 프라이버시를 유지하면서 분산된 데이터로 모델을 훈련하는 새로운 기계 학습 패러다임입니다 (McMahan & Ramage, 2017; Konečný et al., 2016; Bonawitz et al., 2019). Yang et al. (2019)은 FL을 수평(HFL), 수직(VFL), 전이(FTL) 연합 학습으로 분류했습니다.
* **동형 암호화(Homomorphic Encryption, HE)**: 암호화된 데이터에 대해 계산을 수행할 수 있게 하는 암호화 방법으로, 데이터 프라이버시 보호에 널리 사용됩니다 (Rivest et al., 1978; Paillier, 1999; Acar et al., 2018). 특히, 덧셈 동형 암호화(Additively Homomorphic Encryption, AHE)는 효율성으로 인해 많이 활용됩니다.
  * $\text{Enc}(u) + \text{Enc}(v) = \text{Enc}(u+v)$
  * $n \cdot \text{Enc}(u) = \text{Enc}(n \cdot u)$
* **프라이버시 보존 기계 학습**: AHE를 사용하여 암호화된 데이터로 모델을 학습하는 연구가 진행되었습니다 (Aono et al., 2016; Yuan and Yu, 2014).
* **수직 연합 로지스틱 회귀**: Hardy et al. (2017)은 수직 분할된 데이터에 대한 연합 로지스틱 회귀 솔루션을 제안했지만, 이 논문은 제3자 조정자를 제거하고 손실 함수를 근사하지 않으며 대규모 병렬 분산 환경을 목표로 한다는 점에서 차별화됩니다.

## 🛠️ Methodology

### 로지스틱 회귀 개요

로지스틱 회귀는 이진 분류에 널리 사용되며, 예측 확률은 로지스틱 함수로 모델링됩니다:
$$P(y=1|\mathbf{x}; \Theta) = h_{\Theta}(\mathbf{x}) = \frac{1}{1+e^{-\Theta \mathbf{x}}}$$
VFL 환경에서 두 당사자 A와 B는 각각 고유한 특성 $\mathbf{x}_{\text{A}}$, $\mathbf{x}_{\text{B}}$ 및 파라미터 $\Theta_{\text{A}}$, $\Theta_{\text{B}}$를 가집니다. 당사자 A는 또한 레이블 $\mathbf{y}$를 가집니다. 선형 예측자는 $\Theta \mathbf{x} = \Theta_{\text{A}} \mathbf{x}_{\text{A}} + \Theta_{\text{B}} \mathbf{x}_{\text{B}}$로 표현됩니다.
훈련 목표는 다음 손실 함수를 최소화하는 것입니다:
$$L = -\frac{1}{n}\sum_{i=1}^{n} [y_i \log h_{\Theta}(\mathbf{x}_i) + (1-y_i) \log (1-h_{\Theta}(\mathbf{x}_i))]$$
각 당사자의 파라미터에 대한 그래디언트는 다음과 같습니다:
$$\frac{\partial L}{\partial \Theta} = -\frac{1}{n}\sum_{i=1}^{n} (y_i - h_{\Theta}(\mathbf{x}_i))\mathbf{x}_i$$

### 모델 훈련 프로토콜 (제3자 조정자 없음)

모델 훈련 프로토콜은 원본 데이터($\mathbf{x}_{\text{A}}$, $\mathbf{x}_{\text{B}}$, $\mathbf{y}$)와 모델($\Theta_{\text{A}}$, $\Theta_{\text{B}}$)을 각 당사자 내에 안전하게 보호하도록 설계되었습니다. 중간 데이터 교환에는 덧셈 동형 암호화(AHE)가 활용됩니다.

1. **초기화**:
    * **당사자 A**: 암호화 키 쌍을 생성하고 공개 키를 당사자 B에 보냅니다. $\Theta_{\text{A}}$를 초기화합니다.
    * **당사자 B**: $\Theta_{\text{B}}$를 초기화합니다.
2. **선형 예측 계산**:
    * **당사자 B**: 각 인스턴스 $i$에 대해 $\Theta_{\text{B}} \mathbf{x}_{\text{B}}^{(i)}$를 계산하여 당사자 A에 보냅니다.
    * **당사자 A**: $\Theta_{\text{A}} \mathbf{x}_{\text{A}}^{(i)}$를 계산하고, B로부터 받은 $\Theta_{\text{B}} \mathbf{x}_{\text{B}}^{(i)}$와 합하여 전체 선형 예측자 $\Theta \mathbf{x}^{(i)} = \Theta_{\text{A}} \mathbf{x}_{\text{A}}^{(i)} + \Theta_{\text{B}} \mathbf{x}_{\text{B}}^{(i)}$를 계산합니다.
3. **예측 및 잔차 계산**:
    * **당사자 A**: 예측값 $\hat{y}_i = h_{\Theta}(\mathbf{x}_i)$를 계산하고, 잔차 $(y_i - \hat{y}_i)$를 암호화하여 당사자 B에 보냅니다.
4. **그래디언트 계산 및 마스킹**:
    * **당사자 B**: 암호화된 잔차를 사용하여 $\frac{\partial L}{\partial \Theta_{\text{B}}}$를 계산하고, 여기에 난수 마스크 $R_{\text{B}}$를 추가하여 $[\frac{\partial L}{\partial \Theta_{\text{B}}}] + R_{\text{B}}$를 당사자 A에 보냅니다 (여기서 $[\cdot]$는 암호화를 나타냄).
    * **당사자 A**: $\frac{\partial L}{\partial \Theta_{\text{A}}}$와 손실 $L$을 계산합니다.
5. **마스킹 해제 및 그래디언트 복원**:
    * **당사자 A**: 당사자 B로부터 받은 $[\frac{\partial L}{\partial \Theta_{\text{B}}}] + R_{\text{B}}$를 복호화하여 $ \frac{\partial L}{\partial \Theta_{\text{B}}} + R_{\text{B}} $를 얻습니다. $R_{\text{B}}$는 난수 마스크이므로 당사자 B에 다시 $R_{\text{B}}$를 보냅니다.
    * **당사자 B**: 당사자 A로부터 받은 $R_{\text{B}}$를 사용하여 자신의 그래디언트 $ \frac{\partial L}{\partial \Theta_{\text{B}}} $를 복원합니다.
6. **파라미터 업데이트**:
    * **당사자 A**: $\Theta_{\text{A}}$를 업데이트합니다.
    * **당사자 B**: $\Theta_{\text{B}}$를 업데이트합니다.
7. 2~6단계를 수렴 조건이 만족될 때까지 반복합니다.

### 시스템 아키텍처

제안된 솔루션은 각 당사자에서 `worker-parameter server` 아키텍처를 채택합니다 (그림 3 참조).

* **구성**: 각 당사자는 중앙 파라미터 서버(Parameter Server)와 여러 개의 워커 노드(Worker Node)로 구성됩니다.
* **통신**:
  * 워커 노드는 자신에게 해당하는 파라미터 서버와만 통신합니다. 워커 노드 간의 직접적인 연결은 없습니다.
  * 당사자 A와 B 간의 유일한 통신 채널은 각 파라미터 서버 간에 존재합니다.
* **구현 기술**: `ps-lite` 프로젝트를 기반으로 `worker-parameter server` 통신을 구현하고, `brpc`를 사용하여 파라미터 서버 간의 RPC 통신을 구현합니다. 보안을 위한 덧셈 동형 암호화는 `libpaillier`를 활용하며, 대부분의 코드는 C++로 작성되었습니다.

### 보안 분석

이 프로토콜은 "정직하지만 호기심이 많은(honest but curious)" 당사자를 가정하고, 특정 조건에서 당사자들이 상대방의 원본 데이터를 추론할 수 있는 잠재적 위험을 분석합니다.

* **A의 레이블 데이터 보호**: 당사자 B는 암호화된 잔차 $(y_i - \hat{y}_i)$를 받으므로 레이블을 직접 학습할 수 없습니다. 그러나 B의 그래디언트 업데이트에서 잔차의 선형 조합이 사용되므로, 충분한 수의 특성($m \ge n$일 때)과 반복 횟수에서는 이론적으로 잔차를 학습할 위험이 있습니다.
* **B의 비공개 데이터 보호**: 당사자 A는 $\Theta_{\text{B}} \mathbf{x}_{\text{B}}^{(i)}$와 마스킹된 그래디언트 $ \frac{\partial L}{\partial \Theta_{\text{B}}} + R_{\text{B}} $를 받습니다. 알 수 없는 변수($\Theta_{\text{B}}$ 및 $\mathbf{x}_{\text{B}}^{(i)}$)의 수가 방정식의 수보다 많으므로 직접 추론하기 어렵습니다. 특히 그래디언트가 난수로 마스킹되므로 $\mathbf{x}_{\text{B}}^{(i)}$를 추론할 수 없습니다.
* **A의 특성 데이터 보호**: B가 A의 레이블을 학습할 수 있는 시나리오가 발생하면, B는 A의 특성 $\mathbf{x}_{\text{A}}^{(i)}$ 및 파라미터 $\Theta_{\text{A}}$를 추론할 수 있는 잠재적 위험이 있습니다.
결론적으로, 이 솔루션은 **실제 상황에서 신중한 경계 조건 점검을 통해 보안을 확보**할 수 있다고 주장합니다.

## 📊 Results

* **데이터셋**:
  * **Mnist**: 70,000행, 784열의 조밀한 데이터셋. 이진 분류(홀수 vs 짝수)에 맞게 변환.
  * **Citeseer**: 181,395행, 105,354열의 희소 데이터셋.
* **실험 환경**: Baidu 클라우드, 각 머신 16 CPU 코어, 64 GB RAM. 각 당사자에 파라미터 서버 1개와 다수 워커 노드를 배치. 다중 스레딩(8개 스레드) 사용.
* **결과**:
  * **정확도**: 학습된 모델은 Mnist에서 0.95, Citeseer에서 0.99(훈련)/0.89(테스트)의 높은 AUC를 달성하여 우수한 성능을 보였습니다.
  * **수렴**: 모델은 적은 반복 횟수 후 빠르게 안정적인 상태로 수렴했습니다 (그림 4 참조).
  * **확장성 및 효율성**: Citeseer 데이터셋에 대한 실험에서 시스템은 워커 노드 수가 증가함에 따라 준선형(sub-linear) 속도 향상을 보여 우수한 확장성을 입증했습니다 (그림 5 참조).
  * **실용성**: 전체 실행 시간은 기존 로지스틱 회귀 구현과 비교할 만하여, 대규모 분산 데이터에 대한 수직 연합 학습을 처리하는 데 적합함을 보여줍니다.

## 🧠 Insights & Discussion

* **의미**: 본 논문의 해결책은 제3자 조정자가 필요 없으므로, 모든 참여자에게 신뢰받는 조정자를 찾는 현실적인 어려움을 피하고 시스템 배포의 복잡성을 줄입니다. 이는 실제 프로덕션 시스템에서 대규모 분산 데이터에 대한 VFL을 효율적으로 구현하는 데 적합함을 시사합니다.
* **한계 및 향후 연구**:
  * 파라미터 서버의 일부 계산을 워커 노드로 오프로드하면 훈련 시간을 더욱 단축할 수 있을 것으로 예상됩니다.
  * 향후 연구에서는 로지스틱 회귀와 딥러닝(DNN)을 결합하여 VFL에서 은닉 특성을 학습하고 이를 최종 로지스틱 계층에 공급하는 방법을 탐구할 예정입니다.

## 📌 TL;DR

본 논문은 수직 연합 학습(VFL) 환경에서 제3자 조정자 없이 대규모 데이터를 효율적으로 처리하기 위한 병렬 분산 로지스틱 회귀 모델 훈련 시스템을 제안한다. 기존 방식의 제3자 조정자 의존성과 단일 머신 한계를 극복하기 위해, 암호화 기법(AHE)을 활용하여 두 당사자 간의 안전한 직접 통신을 가능하게 하는 새로운 아키텍처와 `worker-parameter server` 기반의 병렬 분산 시스템을 구축했다. 실험 결과, 제안된 시스템은 높은 AUC로 빠르게 수렴하며, 대용량 데이터셋에서 우수한 확장성과 효율성을 입증하여 실제 VFL 시나리오에 적합함을 보여준다.

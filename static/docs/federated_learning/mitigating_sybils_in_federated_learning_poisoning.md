# Mitigating Sybils in Federated Learning Poisoning

Clement Fung, Chris J.M. Yoon, Ivan Beschastnikh

## 🧩 Problem to Solve

페더레이티드 러닝(Federated Learning, FL)은 분산된 다자간 데이터로부터 모델을 훈련하는 데 효과적이지만, 모델 중독(model poisoning) 공격에 취약합니다. 특히 공격자가 시스템 내에서 여러 개의 악성 클라이언트(sybil)를 생성하여 공격의 영향력을 크게 증대시키는 시빌 기반(sybil-based) 중독 공격은 기존 FL 시스템의 보안을 심각하게 위협합니다. 기존 방어 기법들은 공격자의 수를 명시적으로 제한하거나, 클라이언트의 훈련 데이터에 대한 접근 또는 관찰을 가정하는데, 이는 FL의 분산 및 프라이버시 보호 특성상 비현실적입니다. 따라서, 공격자의 수에 대한 가정 없이 시빌 기반 중독 공격을 완화할 수 있는 새로운 방어 메커니즘이 필요합니다.

## ✨ Key Contributions

* 페더레이티드 러닝 아키텍처에서 시빌 공격의 취약성을 평가하고, 기존의 악성 공격 방어 기법(Multi-Krum, RONI 등)이 부적절함을 입증했습니다.
* 클라이언트 간 기여 유사성(inter-client contribution similarity)을 기반으로 클라이언트별 적응형 학습률(adaptive learning rate)을 사용하는 시빌 기반 중독 공격에 대한 새로운 방어 기법인 **FoolsGold**를 설계, 구현 및 평가했습니다.
* 악성 공격자가 수행할 수 있는 최적의 지능형 공격 전략을 논의하고, 추가 완화 방향을 제시했습니다.

## 📎 Related Works

* **Multi-Krum [10]**: 페더레이티드 러닝에서 비잔틴 클라이언트에 대응하기 위해 설계된 기법입니다. 클라이언트 기여 중 평균에서 가장 멀리 떨어진 $f$개의 기여를 제거하는 방식이며, 공격자의 수에 대한 파라미터 $f$를 요구합니다. 본 논문에서는 비IID(non-IID) 설정에서 Multi-Krum이 취약함을 보입니다.
* **RONI (Reject on Negative Influence) [6]**: 의심스러운 훈련 예제에 대한 모델의 성능 저하 여부를 평가하여 중독 공격을 방어합니다. FL 환경에 적용 시 각 반복에서 단일 업데이트의 영향력을 포착하지만, 비IID 데이터로 인해 오탐(false positive)이 발생할 수 있습니다.
* **Secure ML [33, 5]**: SGX와 같은 신뢰 실행 환경이나 데이터 출처(data provenance)를 사용하여 시스템을 더 안전하게 만드는 접근 방식입니다. FoolsGold는 데이터 입력 수준의 중독 공격을 방어하며, SGX 기반 시스템과 함께 사용될 수 있습니다.
* **Sybil Defenses [3, 19, 38, 39, 40, 41]**: 작업 증명(proof of work), 지분 증명(proof of stake), 소셜 네트워크 정보 등 보조 정보를 사용하여 시빌을 탐지하고 제거하는 방어 기법들입니다. FoolsGold는 학습 과정 외의 어떠한 보조 정보도 사용하지 않습니다.
* **AUROR [36]**: 다자간 ML 설정에서 중독 공격을 방어하기 위해 중요한 모델 특징(indicative features)의 수정 분포를 클러스터링하여, 특정 임계값을 초과하는 작은 클러스터의 기여를 제거합니다. FoolsGold는 시빌의 존재를 가정하고 그라디언트 유사성을 사용합니다.

## 🛠️ Methodology

FoolsGold는 페더레이티드 러닝 서버가 클라이언트의 SGD(Stochastic Gradient Descent) 연산 결과(모델 업데이트)만 접근할 수 있는 환경에서 작동합니다. 주요 아이디어는 **정직한 클라이언트와 시빌 클라이언트의 그라디언트 업데이트 다양성**으로 구분할 수 있다는 것입니다. 시빌들은 공통의 악성 목표를 향해 업데이트를 기여하므로, 그들의 업데이트는 정직한 클라이언트보다 더 유사하게 나타날 것으로 예상됩니다.

FoolsGold는 이 가정을 기반으로 각 클라이언트의 학습률($\alpha_i$)을 수정하며, 다음 단계를 따릅니다 (Algorithm 1 참고):

1. **업데이트 이력 유지 및 코사인 유사성 계산**:
    * 각 클라이언트 $i$로부터의 업데이트 $\Delta_{i,t}$를 누적하여 총 이력 벡터 $H_i = \sum_{t=1}^T \Delta_{i,t}$를 유지합니다. 이는 단일 반복에서의 높은 분산으로 인한 오류를 완화합니다.
    * $H_i$와 $H_j$ 간의 코사인 유사성 $\text{cs}_{ij}$를 계산합니다. 이는 유클리드 거리보다 그라디언트 방향에 더 강건하며, 공격자가 업데이트 크기를 조작하는 것을 방지합니다.

2. **특징 중요도(Feature Importance) 고려**:
    * 모델의 출력 레이어 파라미터의 크기를 측정하여 모델 정확도에 중요하고 공격에 의해 수정될 수 있는 '지표적 특징(indicative features)'을 식별합니다. 이 특징들에 가중치를 부여하여 유사성을 계산함으로써, 공격자가 관련 없는 특징에 노이즈를 추가하여 유사성을 낮추는 것을 방지합니다.

3. **용서(Pardoning) 메커니즘**:
    * 정직한 클라이언트가 시빌과 유사한 업데이트를 보낼 때 발생할 수 있는 오탐을 줄이기 위해, $v_i = \max_j(\text{cs}_{ij})$로 정의된 클라이언트 $i$의 최대 쌍별 유사성 값을 활용하여 $\text{cs}_{ij}$를 재가중합니다. 이는 정직한 클라이언트가 부당하게 페널티를 받지 않도록 합니다.

4. **학습률 조정($\alpha_i$)**:
    * 새로운 클라이언트 학습률 $\alpha_i$는 $1 - \max_j(\text{cs}_{ij})$로 계산됩니다.
    * 이 값은 시스템 내에서 가장 높은 $\alpha$ 값으로 정규화되어, 최소한 한 명의 클라이언트가 수정되지 않은 업데이트를 가지도록 보장합니다.

5. **로짓(Logit) 함수 적용**:
    * $\alpha_i$ 값에 로짓 함수 $\kappa(\ln[\frac{\alpha_i}{1-\alpha_i}] + 0.5)$를 적용합니다. 이는 유사성 값의 양 끝에서 더 높은 발산(divergence)을 유도하고, 비제로 유사성을 가진 정직한 클라이언트를 과도하게 페널티하지 않도록 합니다. $\kappa$는 신뢰도 파라미터입니다.

6. **글로벌 모델 업데이트**:
    * 최종적으로 재조정된 학습률 $\alpha_i$를 적용하여 글로벌 모델을 업데이트합니다: $w_{t+1} = w_t + \sum_i \alpha_i \Delta_{i,t}$.

## 📊 Results

FoolsGold는 4가지 데이터셋(MNIST, VGGFace2, KDDCup99, Amazon Reviews)과 3가지 모델 유형(1-layer Softmax, Squeezenet, VGGNet)에 걸쳐 평가되었습니다.

* **정규 공격 시나리오**:
  * 단일 클라이언트 공격($A-1$)을 제외하고, FoolsGold는 대부분의 공격 시나리오(5개 시빌 공격 $A-5$, 동시 다발적 5개 시빌 공격 $A-5 \text{x}5$, KDDCup의 5개 악성 패턴 동시 공격 $A-\text{OnOne}$, 99% 시빌 공격 $A-99$)에서 공격을 효과적으로 방어하며 높은 훈련 정확도를 유지했습니다.
  * $A-1$ 공격에서는 악성 및 정직한 클라이언트를 구별하기 어려워 FoolsGold의 성능이 가장 낮았습니다.
* **기존 연구와의 비교 (Multi-Krum)**:
  * Multi-Krum은 비IID 설정에서 성능이 특히 저조했으며, 정직한 클라이언트의 업데이트 분산이 높고 시빌의 업데이트 분산이 낮을 때 정직한 클라이언트를 제거하는 경향이 있었습니다.
  * Multi-Krum이 33% 이상의 시빌에 대해 실패하는 반면, FoolsGold는 공격자의 수가 증가할수록 공격자를 더 많이 페널티하며, 9명의 공격자에 대해서도 강건함을 유지했습니다.
  * FoolsGold는 FEDSGD와 FEDAVG 모두에서 Multi-Krum보다 우수한 성능을 보였습니다.
* **다양한 클라이언트 데이터 분포**:
  * 비IID($x=0\%$)부터 완전 IID($x=100\%$)까지 다양한 데이터 분포에서도 FoolsGold는 중독 공격 방어에 효과적이었습니다. MNIST와 VGGFace2 데이터셋 모두에서 최대 공격률이 $1\%$ 미만이었습니다. 이는 시빌이 동일한 중독 데이터셋을 가질 때 업데이트가 본질적으로 유사하기 때문입니다.
* **지능형 공격에 대한 강건성**:
  * **지능형 노이즈 추가**: 모델의 지표적 특징에 가중치를 부여하는 메커니즘을 통해 공격자가 노이즈를 추가하여 유사성을 낮추는 것을 방어했습니다. 지표적 특징 비율이 $0.1$ 이하로 떨어지면 지능형 노이즈 공격은 실패했습니다.
  * **적응형 업데이트**: 시빌들이 업데이트 이력의 유사성이 충분히 낮을 때만 중독 업데이트를 보내는 전략은 더 많은 시빌을 요구하며, 공격자는 최적의 공격을 위해 필요한 시빌의 수를 알기 어렵습니다.
* **설계 요소의 효과**:
  * **이력(History)**: 이력 메커니즘을 사용하지 않을 경우, 업데이트 분산이 높은 공격(예: 높은 중독 데이터 비율과 낮은 배치 크기)에 대해 공격률이 크게 증가했습니다.
  * **용서(Pardoning)**: 용서 메커니즘이 없을 경우, 훈련 오류와 대상 클래스 오류가 증가하여 오탐률이 높아졌습니다.
  * **로짓(Logit)**: 로짓 함수가 없으면, 공격자가 압도적인 수의 시빌로 FoolsGold를 무력화할 수 있었습니다.
* **성능 오버헤드**:
  * CPU 환경에서는 클라이언트 수가 증가함에 따라 FoolsGold의 오버헤드가 증가했지만, GPU 기반 딥러닝 모델 훈련에서는 훈련 비용이 높아 FoolsGold로 인한 상대적 성능 저하가 미미했습니다.

## 🧠 Insights & Discussion

FoolsGold는 페더레이티드 러닝 환경에서 시빌 기반 중독 공격을 효과적으로 완화하는 강력한 방어책을 제시합니다. 공격자의 수에 대한 사전 지식 없이 클라이언트 기여의 유사성을 기반으로 작동한다는 점이 핵심적인 강점입니다. 특히, 다양한 데이터 분포, 공격 유형, 그리고 공격자의 지능적인 회피 전략(노이즈 추가, 데이터 혼합, 배치 크기 조작)에도 강건함을 보여주었습니다.

하지만 몇 가지 한계도 존재합니다. FoolsGold는 **단일 클라이언트 공격**에는 취약합니다. 이는 FoolsGold가 여러 시빌 클라이언트 간의 유사성을 탐지하도록 설계되었기 때문입니다. 논문에서는 이러한 경우 Multi-Krum과 같은 기존 방어책과 결합하여 사용할 수 있음을 제안합니다. 또한, 많은 시빌을 가진 고도로 정보에 밝은 공격자가 FoolsGold의 내부 작동 방식을 알고 있다면, 업데이트 전송 시기를 조정하여 방어 메커니즘을 회피할 수 있는 가능성도 있습니다.

미래 연구 방향으로는 FoolsGold의 무작위성(non-determinism)을 높여 정보에 밝은 공격자에게 더 예측 불가능하게 만들거나, 그래프 기반 유사성, 클라이언트 데이터셋의 보조 정보, 또는 최소 유사성 점수를 강제하는 등 더 정교한 유사성 측정 지표를 통합하는 것을 제안합니다.

## 📌 TL;DR

FoolsGold는 페더레이티드 러닝에서 시빌 기반 모델 중독 공격을 막기 위한 새로운 방어 기법입니다. 이 기법은 클라이언트의 그라디언트 업데이트 이력과 특징 중요도를 기반으로 **코사인 유사성**을 측정하여 시빌들을 식별하고, 각 클라이언트의 학습률을 적응적으로 조정합니다. FoolsGold는 공격자의 수에 대한 사전 가정이 필요 없으며, Multi-Krum과 같은 기존 기법보다 뛰어난 성능을 보이며 다양한 공격 유형과 지능형 회피 전략에 강건합니다. 단일 클라이언트 공격에는 취약하지만, 기존 방어책과 결합하여 사용될 수 있습니다.
